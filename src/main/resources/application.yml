server:
  port: 8080

spring:
  application:
    name: webflux-api
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dev-table?serverTimezone=Asia/Seoul
    username: 77kkyu
    password: 1234
    jpa:
      database: mysql
      database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
          format_sql: true